@model CategoriesViewModel
@{
    ViewData["Title"] = "Articles";
}
<form method="PATCH">
@for(int i = 0; i< Model.Categories.Count; i++){
    @Html.EditorFor(m=>Model.Categories[i])
}
<button >Add new category</button>
</form>